version: 2

sources:
  - name: test
    database: analytics
    schema: dbt_jholtzman
    description: "{{ env_var('DBT_TEST') }}"
    tables:
      - name: test_source
        loaded_at_field: _loaded_at
        freshness:
          warn_after:
            count: 24
            period: hour
          error_after:
            count: 48
            period: hour